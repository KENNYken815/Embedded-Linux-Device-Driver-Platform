# Device Tree

Example relationship:

Device Tree node -> kernel driver matching -> Linux device interface -> user-space application

The example GPIO node is illustrative. Controller names, pin numbers and GPIO cells must match the selected board DTS.
