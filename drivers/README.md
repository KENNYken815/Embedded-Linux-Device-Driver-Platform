# Linux Hardware Interfaces

UART: /dev/tty*
GPIO: /dev/gpiochip*
I2C: /dev/i2c-*
SPI: /dev/spidev*

Exact device nodes and permissions depend on the target board, BSP, kernel configuration and Device Tree.
