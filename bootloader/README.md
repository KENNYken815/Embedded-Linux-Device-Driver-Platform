# U-Boot

Reference boot chain:

Boot ROM -> U-Boot -> Linux Kernel + DTB -> RootFS -> systemd -> gateway service

Target-specific boot arguments, storage addresses and console settings must come from the selected board documentation.
