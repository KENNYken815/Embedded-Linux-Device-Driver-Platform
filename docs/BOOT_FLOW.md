# Boot Flow

Power-on -> Boot ROM -> U-Boot -> Linux kernel -> Device Tree -> RootFS -> systemd -> gateway service -> UART telemetry processing

Debug using the serial console, dmesg, journalctl, ps, top and strace.
